<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd" >

<properties>

    <entry key="currentBorrowBooks">
        SELECT
        BOOK_CODE, BOOK_TITLE, BORROW_DATE, DUE_DATE, BR_CREATED_AT, BR_UPDATE_AT
        FROM BORROW_RECORDS
        WHERE USER_ID =? AND BOOK_STATUS = 'FALSE';
    </entry>


    <entry key="updateRequestBook">
        UPDATE BORROW_RECORDS
        SET BOOK_TITLE =?, BOOK_CODE =?, BORROW_DATE = ? , DUE_DATE = ?, BOOK_STATUS = ?, BR_UPDATE_AT =? ;
    </entry>


    <entry key="borrowBookList">
        SELECT
        BOOK_CODE, BOOK_TITLE, BORROW_DATE, DUE_DATE, RETURN_DATE
        FROM BORROW_RECORDS;
    </entry>


    <entry key="pwdUpdate">
        UPDATE USERS
        SET USER_PWD = ? , USER_UPDATED_AT = ?
        WHERE USER_PWD = ? ;
    </entry>


</properties>