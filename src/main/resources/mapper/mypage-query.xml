<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd" >

<properties>

    <entry key="currentBorrowBooks">
        SELECT
            BOOK_CODE, BOOK_TITLE, BORROW_DATE, DUE_DATE
          FROM BORROW_RECORDS
        WHERE USER_ID = ? AND BOOK_STATUS = 'FALSE';
    </entry>


    <entry key="updateRequest1">
        UPDATE BORROW_RECORDS
        SET BOOK_CODE =?, BORROW_DATE =?;
    </entry>


    <entry key="updateRequest2">
        UPDATE BORROW_RECORDS
        SET BOOK_CODE =?, BOOK_STATUS =?;
    </entry>


    <entry key="allBorrowBookList">
        SELECT
        BOOK_CODE, BOOK_TITLE, BORROW_DATE, RETURN_DATE
        FROM BORROW_RECORDS;
    </entry>


    <entry key="pwdUpdate">
        UPDATE USERS
        SET USER_PWD = ?
        WHERE USER_PWD = ? ;
    </entry>


</properties>